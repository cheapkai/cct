    int t;
    cin >> t;
    
    while(t--)
    {
    vector<pair<int ,int>> a;
    int n;cin >> n;
    int k = n;int f;
    while(k--)
        {cin >> f;
        
        if()
        
        if(((k+1)==n) && (f>0))
         a.push_back((f,1));
        
        else if(((a.top()).first < f) && (a.top().second == 0)))
        {
            while((((a.top()).first < f) && (a.top().second == 0))))
            a.pop();
            
            if((a.top().second == 1) && (a.top().first < f)
            a.push_back((f,1));
        
            
        }
        
        else if (a.top().first >= f)
        {
            a.push_back((f,0));
            
        }
        
        
         
            
        }}